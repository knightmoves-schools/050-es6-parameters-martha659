
function calcuateTotal(subtotal, shipping = 2.50 ) => {
    return subtotal + shipping;
};

calculateTotal result = shipping (2.50);

function printTopThreeHeadlines = (...headlines) {
    return headlines.slice(0, 3).join('\n')
};




